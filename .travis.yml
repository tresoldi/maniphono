language: python

python:
  - "3.7"

install:
  - "python setup.py install"
  - "pip install codecov"

script:
  - "coverage run tests/test_phonomodel.py"
  - "coverage run -a tests/test_sound.py"

after_success:
  - "codecov"
